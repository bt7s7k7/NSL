#{"type": "T", "output": ["a","b","c","0","a","1","b","2","c"]}
# Should execute the function for each element in the array

var $arr (arr "a" "b" "c")

$arr.foreach $v => !{ output $v }

$arr.foreach ($v $i) => !{ output ($i.toString); output $v }

